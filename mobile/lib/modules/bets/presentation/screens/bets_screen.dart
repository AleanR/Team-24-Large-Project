import 'package:flutter/material.dart';

class BetsScreen extends StatelessWidget {
  const BetsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Bets screen placeholder'),
      ),
    );
  }
}
